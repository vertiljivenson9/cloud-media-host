{
  "name": "cloud-media-host",
  "version": "1.0.0",
  "description": "Free media hosting on Cloudflare using Google Drive + Cloudinary. No VPS needed.",
  "private": true,
  "scripts": {
    "build": "echo 'Cloudflare Workers no necesita build - se deploya directo'",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "kv:create": "wrangler kv:namespace create MEDIA_KV"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
